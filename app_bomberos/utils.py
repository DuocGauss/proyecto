import requests
'''
def obtener_lista_productos():
    url_otra_pagina_api = 'http://127.0.0.1:8001/api/productos/'
    response = requests.get(url_otra_pagina_api)

    if response.status_code == 200:
        return response.json()
    else:
        # Manejar el error de alguna manera, podrías lanzar una excepción, loggear el error, etc.
        return None
'''